import { ComponentFixture, TestBed } from '@angular/core/testing';

import { First } from './first';

describe('First', () => {

  let x = new First()



  it('should create', () => {
    expect(x.add()).toBe(20)
  });
});
